<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link
  href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700&display=swap"
  rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="text-slate-700 h-full bg-green-100">
  <div class="flex flex-col justify-space-around h-full display-flex">
    <header class="p-6 px-10 bg-slate-800 text-green-100">
      <div class="flex items-center">
        
        <a href="/" class="block p-4 py-2">Home</a>
        <a href="/dashboard" class="block p-4 py-2">Dashboard</a>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <button class="border-none outline-none block p-4 py-2" id="logout">Log out</button>
        {{else}}
        <a href="/login" class="block p-4 py-2">Log in</a>
        <a href="/signup" class="block p-4 py-2">Sign up</a>
        {{/if}}
      </div>
      <h1 class="block ml-4 mt-2 bg-green-100 text-slate-800 rounded-lg p-2 px-4 w-max font-bold drop-shadow-[5px_5px_0_rgb(180,180,180)]">
        <a href="/" class="text-lg" style="">
          {{#if page_title}}
          {{page_title}}
          {{else}}
          The Tech Blog
          {{/if}}
        </a>
      </h1>
    </header>
    <main class="w-full mt-5 flex-1">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
